#version 100

varying mediump vec4 oFogColor;
varying mediump vec4 color;
varying mediump vec2 texCoord;

void main(void)
{
	gl_FragColor = vec4(mix(oFogColor.rgb, color.rgb, oFogColor.a), color.a + texCoord.x * 0.001);
}
